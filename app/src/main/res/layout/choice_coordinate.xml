<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/rounded_corner_background"
    android:padding="16dp"
    android:layout_gravity="center"
    android:elevation="4dp"
    android:layout_margin="16dp">

    <TextView
        android:id="@+id/coordinate_input_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="좌표 입력 방식"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/coordinate_input_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="좌표 입력 방식을 선택해주세요. 소수점 형식(DD) 또는 도분초 형식(DMS)을 사용할 수 있습니다."
        android:textColor="@color/black"
        android:textSize="14sp"
        android:layout_marginTop="8dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/btn_dd_format"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:textStyle="bold"
            android:text="DD (위도, 경도)"
            android:textColor="@color/secondaryColor" /> <!-- 글자색 변경 -->

        <Button
            android:id="@+id/btn_dms_format"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:text="DMS (도분초)"
            android:textStyle="bold"
            android:textColor="@color/primaryColor" /> <!-- 글자색 변경 -->
    </LinearLayout>
</LinearLayout>
